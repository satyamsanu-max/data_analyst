import type { SeedQuestion } from "./types";

/** Machine Learning bank (cap: 120, shares the daily ML/Python slot with Python). */

type Row = [
  topic: string,
  title: string,
  difficulty: "Easy" | "Medium" | "Hard",
  minutes: number,
  frequency: number,
  patternValue: number,
  companies: string,
  concepts: string,
  prompt: string,
  solution: string,
  explanation: string,
];

const ROWS: Row[] = [
  // ---------------- LINEAR MODELS ----------------
  ["ml-linear-models", "Assumptions of linear regression", "Medium", 20, 94, 92, "amazon,meta,american-express,mckinsey", "linearity,independence,homoscedasticity,normality",
    "List the assumptions of ordinary least squares and explain what breaks when each is violated.",
    "Linearity, independent errors, constant error variance, approximately normal residuals for small-sample inference, and no perfect multicollinearity. Violations bias the fit, invalidate standard errors, or destabilise coefficients respectively.",
    "Note which violations harm predictions versus which only harm inference. Heteroscedasticity, for instance, leaves coefficients unbiased but makes p-values wrong."],
  ["ml-linear-models", "Why logistic regression not linear regression", "Medium", 20, 96, 94, "amazon,meta,google,american-express,flipkart", "logistic regression,link function,bounded output",
    "Why not just fit a linear regression to a binary outcome?",
    "Linear regression can predict below zero and above one, its errors are heteroscedastic by construction, and the linear probability assumption is usually wrong at the extremes. Logistic regression models the log-odds, which maps any real number to a valid probability.",
    "Mention that the linear probability model is still used in econometrics for interpretable marginal effects, which shows breadth."],
  ["ml-linear-models", "Interpreting logistic coefficients", "Medium", 20, 92, 92, "amazon,meta,american-express,jpmorgan", "log-odds,odds ratio,interpretation",
    "A logistic model gives a coefficient of 0.7 for a feature. What does that mean in plain language?",
    "A one-unit increase multiplies the odds by e^0.7 which is about 2.0, holding everything else constant. It does not double the probability.",
    "Coefficients live in log-odds space. The effect on probability depends on where you start, which is the nuance being tested."],
  ["ml-linear-models", "Cost function of logistic regression", "Medium", 20, 82, 90, "amazon,google,meta", "log loss,cross-entropy,convexity",
    "Why does logistic regression use log loss rather than squared error?",
    "Squared error is non-convex when composed with the sigmoid, so optimisation can get stuck. Log loss is convex in the parameters and is the negative log-likelihood of the Bernoulli model, giving it a principled statistical basis.",
    "Log loss also punishes confident wrong predictions severely, which is exactly the behaviour you want from a probability model."],
  ["ml-linear-models", "Gradient descent explained", "Medium", 20, 84, 88, "amazon,google,meta,microsoft", "gradient descent,learning rate,convergence",
    "Explain gradient descent and what happens when the learning rate is too large or too small.",
    "Iteratively step against the gradient of the loss. Too large and the updates overshoot and diverge; too small and convergence is slow and may stall in a flat region.",
    "Mention batch, stochastic, and mini-batch variants, and note that feature scaling strongly affects convergence speed."],
  ["ml-linear-models", "Normal equation versus gradient descent", "Medium", 20, 66, 82, "amazon,google", "closed form,computational cost",
    "When would you solve linear regression in closed form rather than iteratively?",
    "The closed form is exact and needs no tuning, but inverting the feature matrix costs roughly cubic time in the number of features. Use it for small feature counts; use gradient descent when features or rows are numerous.",
    "The crossover is usually around ten thousand features. Knowing the cubic cost is the key detail."],
  ["ml-linear-models", "Polynomial and interaction terms", "Medium", 20, 70, 84, "amazon,american-express,mckinsey", "feature engineering,nonlinearity,interactions",
    "Your linear model underfits a clearly curved relationship. What are your options?",
    "Add polynomial terms, spline basis functions, or interaction terms; transform the target with a log; or switch to a model that captures nonlinearity natively such as a gradient boosted tree.",
    "Linear models are linear in parameters, not in features. Engineering the right basis is often enough."],

  // ---------------- TREES & ENSEMBLES ----------------
  ["ml-trees", "How a decision tree splits", "Medium", 20, 90, 92, "amazon,meta,flipkart,walmart", "gini,entropy,information gain",
    "How does a decision tree decide where to split, and what do Gini and entropy measure?",
    "It evaluates candidate splits and picks the one that most reduces impurity in the children. Gini measures the chance of misclassifying a random sample; entropy measures information content. They rarely disagree in practice, and Gini is cheaper to compute.",
    "For regression trees the criterion becomes variance reduction instead. Mentioning that completes the answer."],
  ["ml-trees", "Random forest versus a single tree", "Medium", 20, 92, 92, "amazon,meta,walmart,american-express", "bagging,variance reduction,decorrelation",
    "Why does a random forest outperform a single deep tree?",
    "A deep tree has low bias and very high variance. Bagging averages many trees fit on bootstrap samples, and the random feature subset at each split decorrelates them so the averaging actually reduces variance.",
    "The feature subsampling is the part candidates forget. Without it, correlated trees would average to almost the same thing."],
  ["ml-trees", "Bagging versus boosting", "Medium", 20, 94, 96, "amazon,meta,google,flipkart,american-express", "bagging,boosting,bias-variance",
    "Contrast bagging and boosting in terms of what error component each attacks.",
    "Bagging fits independent models in parallel on resampled data and mainly reduces variance. Boosting fits models sequentially, each correcting the previous residuals, and mainly reduces bias. Boosting is more accurate but more prone to overfitting and more sensitive to noise.",
    "Framing it as variance versus bias is the answer interviewers are listening for."],
  ["ml-trees", "How gradient boosting works", "Medium", 20, 90, 94, "amazon,meta,walmart,flipkart", "gradient boosting,residuals,additive model",
    "Explain gradient boosting to someone who knows decision trees.",
    "Start with a constant prediction. Repeatedly fit a shallow tree to the negative gradient of the loss with respect to the current predictions, and add it scaled by a learning rate. The ensemble is an additive model built greedily.",
    "For squared error the negative gradient is just the residual, which makes the intuition concrete."],
  ["ml-trees", "XGBoost specifics", "Medium", 20, 82, 88, "amazon,walmart,flipkart,american-express", "regularisation,shrinkage,sparsity handling",
    "What does XGBoost add over plain gradient boosting?",
    "Explicit L1 and L2 regularisation in the objective, second-order gradient information, built-in handling of missing values via default split directions, column and row subsampling, and an efficient histogram-based split finder.",
    "The regularised objective is the conceptual addition; the rest are engineering. Making that distinction is a strong answer."],
  ["ml-trees", "Key hyperparameters for boosting", "Medium", 20, 84, 90, "amazon,walmart,flipkart", "hyperparameters,tuning,overfitting",
    "Which boosting hyperparameters matter most and how do they interact?",
    "Learning rate and number of trees trade off directly: halve the rate and roughly double the trees. Max depth or leaf count controls model complexity. Subsample and column sample add regularisation. Min child weight prevents tiny overfitted leaves.",
    "Tuning learning rate and tree count independently is a classic mistake. They must be tuned together with early stopping."],
  ["ml-trees", "Feature importance pitfalls", "Medium", 20, 80, 90, "amazon,meta,american-express", "impurity importance,permutation importance,bias",
    "Why can tree-based feature importance mislead you?",
    "Impurity importance is biased toward high-cardinality and continuous features, and it splits credit arbitrarily among correlated features. Use permutation importance on held-out data, or SHAP values, instead.",
    "This is a common follow-up after any random forest question. Naming permutation importance and SHAP is expected."],
  ["ml-trees", "When trees beat linear models and vice versa", "Medium", 20, 84, 90, "amazon,meta,american-express,mckinsey", "model selection,tabular data,extrapolation",
    "When would you choose a linear model over a gradient boosted tree?",
    "When you need extrapolation beyond the training range, when coefficients must be defensible to regulators, when data is small and low-dimensional, or when a smooth monotonic relationship is known. Trees cannot extrapolate at all.",
    "The extrapolation point is the sharpest one. Trees predict a constant outside the training range."],

  // ---------------- OTHER MODELS ----------------
  ["ml-other-models", "How KNN works and its weaknesses", "Medium", 20, 74, 82, "amazon,flipkart,adobe", "KNN,distance metrics,curse of dimensionality",
    "Explain KNN and describe when it fails.",
    "Predict from the k nearest training points by distance. It fails in high dimensions where all distances converge, it is slow at prediction time since there is no training phase, and it requires feature scaling or the largest-scale feature dominates the distance.",
    "The curse of dimensionality point is essential. Also note there is no model to inspect, so it is uninterpretable."],
  ["ml-other-models", "Choosing k in KNN", "Easy", 15, 62, 76, "amazon,adobe", "hyperparameter,bias-variance",
    "How do you pick k, and what does the choice do to bias and variance?",
    "Cross-validate. Small k means low bias and high variance and it fits noise; large k means high bias and low variance and it smooths over real structure. Use an odd k for binary classification to avoid ties.",
    "This is a clean, concrete illustration of the bias-variance tradeoff."],
  ["ml-other-models", "Support vector machines", "Medium", 20, 66, 80, "amazon,google,adobe", "margin,kernel trick,support vectors",
    "What is an SVM optimising, and what is the kernel trick?",
    "It maximises the margin between classes, defined only by the support vectors. The kernel trick computes inner products in a high-dimensional space without ever constructing it, letting a linear separator work on nonlinear data.",
    "SVMs shine on small, high-dimensional data. They scale poorly to large datasets, which is why boosting displaced them for tabular work."],
  ["ml-other-models", "Naive Bayes and its assumption", "Medium", 20, 70, 84, "amazon,meta,adobe", "Naive Bayes,conditional independence,text classification",
    "What makes Naive Bayes naive, and why does it work anyway?",
    "It assumes features are conditionally independent given the class, which is almost always false. It still works because classification only needs the correct argmax, not calibrated probabilities, and the errors often do not change which class wins.",
    "Its probabilities are badly calibrated, so never use them as real probabilities without recalibration."],

  // ---------------- UNSUPERVISED ----------------
  ["ml-unsupervised", "K-means and choosing k", "Medium", 20, 84, 88, "amazon,meta,spotify,flipkart", "k-means,elbow method,silhouette",
    "Explain k-means and describe how to choose the number of clusters.",
    "Alternate between assigning points to the nearest centroid and recomputing centroids until stable. Choose k with the elbow of the inertia curve, silhouette score, or gap statistic, tempered by whether the clusters are actionable for the business.",
    "K-means assumes roughly spherical, similarly sized clusters and is sensitive to initialisation, which is why k-means++ exists."],
  ["ml-unsupervised", "When k-means fails", "Medium", 20, 72, 86, "amazon,meta,spotify", "cluster shape,DBSCAN,scaling",
    "Your clusters are elongated and of very different densities. What now?",
    "K-means will carve them wrongly because it assumes spherical equal-variance clusters. Use DBSCAN for density-based shapes, Gaussian mixtures for elliptical clusters, or hierarchical clustering when you want a dendrogram.",
    "Always scale features first. Unscaled features let one variable dictate the entire distance metric."],
  ["ml-unsupervised", "PCA explained", "Medium", 20, 86, 90, "amazon,meta,google,american-express", "PCA,variance,orthogonal components",
    "Explain PCA and what the components represent.",
    "PCA finds orthogonal directions of maximum variance in the data. The first component captures the most variance, each subsequent one the most of what remains. Projecting onto the top few reduces dimensions while keeping most of the variance.",
    "Standardise first or high-variance features dominate. Components are linear combinations and usually lack a clean business meaning."],
  ["ml-unsupervised", "PCA versus feature selection", "Medium", 20, 70, 86, "amazon,american-express,mckinsey", "dimensionality reduction,interpretability",
    "When would you prefer feature selection over PCA?",
    "When you need to explain the model, when data collection cost matters and you want to stop measuring some inputs, or when only a few original features carry signal. PCA mixes all features into every component, destroying interpretability.",
    "PCA is unsupervised and ignores the target, so a low-variance direction that predicts well can be discarded."],
  ["ml-unsupervised", "Customer segmentation in practice", "Medium", 20, 78, 86, "flipkart,amazon,mckinsey,bcg", "segmentation,RFM,business application",
    "How would you build a customer segmentation for an e-commerce business?",
    "Start with RFM features: recency, frequency, and monetary value, plus category mix. Scale, reduce dimensions if needed, cluster, then profile each cluster and check that the segments are distinguishable, stable over time, and actionable for marketing.",
    "The evaluation criterion is business usefulness, not silhouette score. Say so explicitly."],

  // ---------------- FEATURES & DATA PREP ----------------
  ["ml-features", "Handling missing data", "Medium", 20, 92, 94, "amazon,meta,walmart,american-express,accenture", "imputation,missingness mechanism,indicator variables",
    "Walk through your approach to a feature that is 30 percent missing.",
    "First ask why it is missing: missing completely at random, at random given other variables, or not at random. Then choose: drop if the column is useless, impute with median or a model, or add a missingness indicator alongside the imputed value. Tree models such as XGBoost can handle it natively.",
    "The missingness itself is often predictive. Adding an indicator column preserves that signal, which mean imputation alone destroys."],
  ["ml-features", "Encoding categorical variables", "Medium", 20, 90, 92, "amazon,meta,walmart,flipkart", "one-hot,target encoding,cardinality",
    "How do you encode a categorical feature with 5000 distinct values?",
    "One-hot is impractical. Use target or mean encoding computed inside cross-validation folds to avoid leakage, hashing for a fixed width, frequency encoding, or an embedding. Group rare levels into an other bucket.",
    "Target encoding leaks badly if computed on the full training set. Fold-wise computation with smoothing is the correct approach."],
  ["ml-features", "Feature scaling: when it matters", "Medium", 20, 84, 90, "amazon,meta,adobe", "standardisation,normalisation,model sensitivity",
    "Which models need feature scaling and which do not?",
    "Distance and gradient based models need it: KNN, SVM, k-means, PCA, neural nets, and regularised linear models. Tree-based models do not, since splits are invariant to monotonic transformations.",
    "Note that regularised linear models need scaling because the penalty applies equally to all coefficients regardless of their natural units."],
  ["ml-features", "Skewed features and transformations", "Medium", 20, 76, 86, "amazon,american-express,stripe", "log transform,Box-Cox,skew",
    "A revenue feature is heavily right-skewed. What do you do?",
    "Apply a log or log1p transform, or a Box-Cox transform if zeros are absent. For tree models it is unnecessary since splits do not care about monotonic scaling.",
    "Knowing when a transform is pointless is as valuable as knowing when to apply one."],
  ["ml-features", "Creating features from timestamps", "Medium", 20, 84, 90, "uber,doordash,swiggy,amazon", "datetime features,cyclical encoding,recency",
    "What features would you derive from a raw event timestamp?",
    "Hour, day of week, month, is-weekend, is-holiday, time since the user's previous event, time since signup, and cyclical sine and cosine encodings of hour and day so that 23:00 and 00:00 are close.",
    "The cyclical encoding detail separates candidates who have actually built time features from those who have only read about them."],
  ["ml-features", "Aggregate features for entity-level models", "Medium", 20, 80, 90, "amazon,flipkart,swiggy,stripe", "aggregation,windowed features,leakage",
    "You have transaction rows but must predict per customer. How do you build features?",
    "Aggregate to the customer grain: counts, sums, means, and standard deviations over trailing windows of 7, 30, and 90 days, ratios between windows to capture trend, recency, and diversity counts. Every window must end before the prediction time.",
    "The cut-off discipline is what prevents leakage. Say it explicitly, because the interviewer is waiting for it."],
  ["ml-features", "Curse of dimensionality", "Medium", 20, 72, 86, "amazon,google,meta", "dimensionality,sparsity,sample complexity",
    "What is the curse of dimensionality and how does it affect modelling?",
    "As dimensions grow, data becomes sparse, all pairwise distances converge, and the sample size needed for reliable estimation grows exponentially. Distance-based methods degrade first.",
    "The practical response is dimensionality reduction, regularisation, or feature selection."],

  // ---------------- EVALUATION ----------------
  ["ml-evaluation", "Precision versus recall", "Medium", 20, 98, 98, "amazon,meta,google,american-express,stripe,flipkart", "precision,recall,tradeoff,business cost",
    "Define precision and recall and give a scenario where each should dominate.",
    "Precision is the share of predicted positives that are correct. Recall is the share of actual positives that were caught. Favour precision when false positives are expensive, such as blocking legitimate transactions. Favour recall when misses are expensive, such as screening for a serious disease.",
    "Anchor the choice to the cost of each error type. Reciting definitions without that framing is a weak answer."],
  ["ml-evaluation", "Why accuracy fails on imbalanced data", "Easy", 15, 96, 94, "amazon,meta,american-express,stripe", "accuracy paradox,imbalance,baseline",
    "Your fraud model is 99.5 percent accurate. Why might that be worthless?",
    "If fraud is 0.5 percent of transactions, predicting never-fraud scores 99.5 percent accuracy while catching nothing. Report precision, recall, and PR-AUC against the base rate instead.",
    "Always compare against the majority-class baseline. This is the fastest way to expose a meaningless accuracy number."],
  ["ml-evaluation", "ROC-AUC explained", "Medium", 20, 94, 94, "amazon,meta,google,american-express", "ROC,AUC,threshold independence",
    "What does an AUC of 0.85 actually mean?",
    "Given one random positive and one random negative, the model ranks the positive higher 85 percent of the time. It is threshold independent and measures ranking quality, not calibration.",
    "The pairwise ranking interpretation is the one that impresses. AUC says nothing about whether predicted probabilities are accurate."],
  ["ml-evaluation", "ROC-AUC versus PR-AUC", "Medium", 20, 90, 94, "amazon,meta,stripe,american-express", "PR curve,imbalance,baseline",
    "When should you prefer PR-AUC over ROC-AUC?",
    "Under heavy class imbalance. ROC uses the false positive rate, whose denominator is the huge negative class, so it stays optimistic. The precision-recall curve focuses on the rare positive class and reflects real performance better.",
    "The PR baseline is the positive class prevalence, not 0.5. Stating that shows you have read the curve correctly."],
  ["ml-evaluation", "F1 and its variants", "Medium", 20, 84, 88, "amazon,meta,flipkart", "F1,harmonic mean,F-beta",
    "Why is F1 a harmonic mean, and when would you use F-beta instead?",
    "The harmonic mean punishes imbalance between precision and recall, so a model cannot score well by maximising one. Use F-beta with beta above 1 to weight recall higher, or below 1 to weight precision higher.",
    "F1 implicitly assumes precision and recall matter equally, which is rarely true in business. F-beta makes the weighting explicit."],
  ["ml-evaluation", "Choosing a classification threshold", "Medium", 20, 90, 94, "amazon,american-express,stripe,jpmorgan", "threshold tuning,cost matrix,expected value",
    "Your model outputs probabilities. How do you pick the cutoff?",
    "Not at 0.5 by default. Build a cost matrix for false positives and false negatives, then choose the threshold minimising expected cost. Alternatively fix the threshold to hit an operational constraint, such as reviewing only the top 1000 cases per day.",
    "Tying the threshold to capacity or to the cost ratio is what a real deployment looks like."],
  ["ml-evaluation", "Calibration", "Medium", 20, 76, 90, "amazon,stripe,american-express,jpmorgan", "calibration,reliability curve,Platt scaling",
    "What does it mean for a model to be well calibrated and how do you check?",
    "Among cases predicted at 30 percent, about 30 percent should actually be positive. Check with a reliability diagram or the Brier score. Fix miscalibration with Platt scaling or isotonic regression.",
    "Calibration matters whenever the probability itself drives a decision, such as expected loss pricing. Boosted trees are usually overconfident."],
  ["ml-evaluation", "Regression metrics", "Medium", 20, 84, 88, "amazon,walmart,uber,flipkart", "RMSE,MAE,MAPE,metric choice",
    "Compare RMSE, MAE, and MAPE and say when each is appropriate.",
    "RMSE punishes large errors heavily and is sensitive to outliers. MAE weights all errors equally and is robust. MAPE is scale free and readable but explodes near zero actuals and asymmetrically penalises overprediction.",
    "For demand forecasting with occasional zeros, MAPE is a trap. Suggest WAPE or MASE instead."],
  ["ml-evaluation", "Confusion matrix from scratch", "Easy", 15, 88, 88, "amazon,meta,flipkart,walmart", "confusion matrix,derived metrics",
    "From TP 80, FP 20, FN 40, TN 860, compute accuracy, precision, recall, F1, and specificity.",
    "Accuracy = 940/1000 = 0.94. Precision = 80/100 = 0.80. Recall = 80/120 = 0.667. F1 = 2(0.8)(0.667)/(1.467) = 0.727. Specificity = 860/880 = 0.977.",
    "Being able to do this quickly and without hesitation is table stakes in an analytics screen."],
  ["ml-evaluation", "Offline metrics versus online outcomes", "Medium", 20, 82, 92, "meta,spotify,netflix,uber", "offline evaluation,online metrics,gap",
    "Your model improves offline AUC but the A/B test shows no lift. What could explain it?",
    "Feedback loops and position bias in logged data, a training-serving distribution mismatch, a metric that does not map to user value, latency degradation, or a downstream system that ignores the ranking change.",
    "The offline-online gap is a real production problem, so this question separates people who have shipped models from those who have not."],

  // ---------------- VALIDATION & LEAKAGE ----------------
  ["ml-validation", "Data leakage", "Medium", 20, 96, 98, "amazon,meta,google,stripe,american-express", "leakage,target leakage,train-test contamination",
    "Define data leakage and give three concrete examples.",
    "Information available at training time that would not be available at prediction time. Examples: a feature computed after the outcome, such as refund amount when predicting churn; scaling or imputing before the train-test split; and target encoding computed on the full dataset.",
    "The tell-tale sign is implausibly high validation performance. Leakage is the most common cause of models that fail in production."],
  ["ml-validation", "Cross-validation", "Medium", 20, 92, 92, "amazon,meta,google,walmart", "k-fold,stratification,variance of estimate",
    "Explain k-fold cross-validation and when you would stratify.",
    "Split into k folds, train on k-1 and validate on the held-out fold, rotating through all folds and averaging. Stratify when classes are imbalanced so each fold preserves the class ratio.",
    "Cross-validation gives a lower-variance performance estimate than a single split, and the fold-to-fold spread itself is useful information."],
  ["ml-validation", "Time series validation", "Medium", 20, 90, 96, "uber,doordash,amazon,stripe,walmart", "temporal split,forward chaining,leakage",
    "Why is standard k-fold wrong for time series, and what do you use instead?",
    "Random folds let the model train on the future and predict the past, which leaks. Use forward chaining: train on everything up to time t and validate on the next window, rolling forward. Include a gap when features use trailing windows.",
    "Any forecasting or churn question should trigger this answer immediately. It is a frequent silent disqualifier."],
  ["ml-validation", "Group-aware splitting", "Medium", 20, 78, 92, "meta,uber,doordash,stripe", "GroupKFold,entity leakage",
    "Each user contributes many rows. What is wrong with a random train-test split?",
    "The same user appears on both sides, so the model memorises user-specific quirks and validation is optimistic. Split by user with GroupKFold so every user lives entirely in one fold.",
    "Entity leakage is subtle and extremely common in transaction-level data."],
  ["ml-validation", "Train, validation and test", "Easy", 15, 86, 88, "amazon,meta,google", "three-way split,model selection,honest estimate",
    "Why do you need three splits rather than two?",
    "Validation is used repeatedly for tuning, so performance on it becomes optimistic through selection. The test set is touched once at the end to give an honest estimate.",
    "Every time you look at the test set and change something, it becomes another validation set."],
  ["ml-validation", "Nested cross-validation", "Hard", 25, 62, 88, "google,meta,amazon", "nested CV,unbiased estimate,tuning",
    "When is nested cross-validation worth the cost?",
    "When you need an unbiased estimate of performance for a procedure that itself includes hyperparameter tuning, especially with small data where a dedicated test set would be too small.",
    "The inner loop tunes and the outer loop evaluates. It is expensive, so reserve it for small datasets and publication-grade claims."],
  ["ml-validation", "Distribution shift monitoring", "Medium", 20, 80, 90, "amazon,stripe,uber,netflix", "drift,PSI,monitoring",
    "How would you detect that a deployed model has gone stale?",
    "Monitor input feature distributions with population stability index or KS tests, monitor the prediction distribution, and track realised performance once labels arrive. Alert on drift and schedule retraining.",
    "Distinguish covariate shift from concept drift. The first changes inputs, the second changes the input-to-output relationship."],

  // ---------------- REGULARIZATION & BIAS-VARIANCE ----------------
  ["ml-regularization", "Bias-variance tradeoff", "Medium", 20, 96, 96, "amazon,meta,google,american-express,flipkart", "bias,variance,irreducible error",
    "Decompose prediction error and explain the tradeoff.",
    "Expected error = bias squared + variance + irreducible noise. High bias means underfitting and the model is too simple. High variance means overfitting and the model memorises noise. Reducing one typically raises the other.",
    "Note that modern ensembles can reduce both at once, so the tradeoff is a guide rather than a law."],
  ["ml-regularization", "L1 versus L2 regularisation", "Medium", 20, 94, 96, "amazon,meta,google,american-express", "lasso,ridge,sparsity",
    "How do L1 and L2 penalties differ in effect?",
    "L1 (lasso) drives some coefficients exactly to zero, performing feature selection. L2 (ridge) shrinks all coefficients toward zero without eliminating them and handles correlated predictors more gracefully. Elastic net combines both.",
    "The geometric explanation, that the L1 constraint region has corners on the axes, is the answer that stands out."],
  ["ml-regularization", "Diagnosing overfitting", "Easy", 15, 92, 92, "amazon,meta,walmart,flipkart", "overfitting,learning curves,remedies",
    "Training accuracy is 99 percent and validation is 72 percent. What is happening and what do you do?",
    "Classic overfitting. Simplify the model, add regularisation, gather more data, apply early stopping, use cross-validation, or remove noisy features. Also check for leakage in the opposite direction.",
    "Plotting learning curves tells you whether more data would actually help or whether the model itself is the problem."],
  ["ml-regularization", "Diagnosing underfitting", "Easy", 15, 82, 86, "amazon,meta,flipkart", "underfitting,capacity,features",
    "Both training and validation error are high and close together. What now?",
    "The model lacks capacity. Add features or interactions, increase model complexity, reduce regularisation, or train longer.",
    "The tell is that the two curves converge at a poor level. That rules out variance as the problem."],
  ["ml-regularization", "Early stopping as regularisation", "Medium", 20, 74, 86, "amazon,google,meta", "early stopping,implicit regularisation",
    "Why does early stopping act like regularisation?",
    "Stopping before convergence limits how far parameters travel from their initialisation, constraining the effective model complexity in much the same way an explicit penalty would.",
    "For boosting, early stopping on a validation set is the single most important guard against overfitting."],
  ["ml-regularization", "Dropout and ensembling intuition", "Medium", 20, 62, 80, "google,meta,amazon", "dropout,neural networks,implicit ensemble",
    "Explain dropout and why it helps.",
    "Randomly zero a fraction of units during training. This prevents co-adaptation between units and approximates averaging over an exponential family of thinned networks, which reduces variance.",
    "At inference time activations are scaled so the expected output matches training."],

  // ---------------- CLASS IMBALANCE ----------------
  ["ml-imbalance", "Handling class imbalance", "Medium", 20, 92, 94, "amazon,american-express,stripe,jpmorgan", "resampling,class weights,SMOTE",
    "Fraud is 0.3 percent of transactions. Walk through your options.",
    "Use class weights or scale_pos_weight rather than touching the data first. Consider undersampling the majority or SMOTE for the minority, always applied inside the training fold only. Choose PR-AUC as the metric and tune the threshold by expected cost.",
    "The most important point: never resample before splitting, and never resample the validation set. That inflates results dramatically."],
  ["ml-imbalance", "SMOTE and its limits", "Medium", 20, 74, 88, "amazon,american-express,stripe", "SMOTE,synthetic samples,overfitting",
    "When is SMOTE a bad idea?",
    "With high-dimensional or categorical data where interpolation between neighbours produces implausible records, when the minority class is genuinely multimodal, or when noisy minority points get amplified. Class weights are often simpler and just as effective.",
    "SMOTE also distorts the base rate, so predicted probabilities need recalibration afterwards."],
  ["ml-imbalance", "Evaluating imbalanced models", "Medium", 20, 86, 92, "amazon,stripe,american-express", "PR-AUC,recall at k,lift",
    "Which metrics do you report for a rare-event model?",
    "PR-AUC, precision and recall at the operating threshold, recall at a fixed review capacity such as top 1000, and lift over the base rate. Accuracy and ROC-AUC alone are misleading.",
    "Recall at a fixed capacity is the metric operations teams actually care about, since review headcount is finite."],
  ["ml-imbalance", "Anomaly detection versus classification", "Medium", 20, 68, 86, "amazon,stripe,jpmorgan", "unsupervised anomaly detection,labels",
    "You suspect fraud but have almost no labels. What approach do you take?",
    "Start with unsupervised anomaly detection such as isolation forest or an autoencoder reconstruction error, use the flags to seed manual review, and bootstrap a labelled set to move to supervised learning over time.",
    "Describing the path from unsupervised to supervised as labels accumulate is the mature answer."],

  // ---------------- INTERPRETABILITY ----------------
  ["ml-interpretability", "SHAP values", "Medium", 20, 80, 90, "amazon,american-express,jpmorgan,meta", "SHAP,Shapley values,local explanation",
    "What are SHAP values and what makes them attractive?",
    "They allocate a prediction's deviation from the base value among features using Shapley values from cooperative game theory. They are locally accurate, consistent, and work for both individual predictions and global summaries.",
    "The consistency guarantee is what distinguishes SHAP from ad hoc importance measures. Exact computation is expensive, so TreeSHAP is used for tree models."],
  ["ml-interpretability", "Explaining a model to a business stakeholder", "Medium", 20, 84, 88, "amazon,mckinsey,bcg,american-express", "communication,partial dependence,storytelling",
    "A VP asks why the model flagged a specific customer. How do you answer?",
    "Give the top three contributing factors in business language with their direction and rough magnitude, compare the customer to a typical one, and state the model's confidence and its known limitations. Avoid coefficients and jargon.",
    "Communication is graded as heavily as modelling in analyst interviews. Lead with the decision, then the evidence."],
  ["ml-interpretability", "Global versus local interpretability", "Medium", 20, 68, 84, "amazon,american-express,jpmorgan", "PDP,ICE,local versus global",
    "Distinguish global and local explanation methods and name one of each.",
    "Global methods describe overall behaviour: partial dependence plots and permutation importance. Local methods explain one prediction: SHAP and LIME. Individual conditional expectation plots sit in between.",
    "Partial dependence plots assume feature independence and mislead when features are correlated. ICE plots reveal the heterogeneity PDPs average away."],
  ["ml-interpretability", "Fairness in a scoring model", "Hard", 25, 70, 88, "amazon,jpmorgan,american-express,google", "fairness,disparate impact,tradeoffs",
    "How would you check whether a credit model treats groups unfairly?",
    "Compare approval rates, false positive and false negative rates, and calibration across protected groups. Recognise that demographic parity, equalised odds, and calibration cannot generally all hold at once, so the business must choose which definition applies.",
    "The impossibility result is the key technical point. Also note that removing the protected attribute does not remove the bias, since proxies remain."],
];

export const ML_QUESTIONS: SeedQuestion[] = ROWS.map((r, i) => {
  const [topic, title, difficulty, minutes, frequency, patternValue, companies, concepts, prompt, solution, explanation] = r;
  return {
    id: `ml-${String(i + 1).padStart(3, "0")}`,
    category: "ML",
    topic,
    title,
    difficulty,
    estimatedMinutes: minutes,
    source: "scikit-learn docs",
    sourceUrl: "https://scikit-learn.org/stable/user_guide.html",
    sourceNote: "Standard interview concept; wording and worked answer are original to this app.",
    companyTags: companies ? companies.split(",") : [],
    frequencyScore: frequency,
    patternValue,
    conceptCoverage: Math.round((frequency + patternValue) / 2),
    concepts: concepts.split(","),
    skillsTested: concepts.split(","),
    prompt,
    solution,
    explanation,
  };
});
