import type { SeedQuestion } from "./types";

/** Python bank (cap: 120, shares the daily ML/Python slot with ML). */

type Row = [
  topic: string,
  title: string,
  difficulty: "Easy" | "Medium" | "Hard",
  minutes: number,
  frequency: number,
  patternValue: number,
  companies: string,
  concepts: string,
  prompt: string,
  solution: string,
  explanation: string,
];

const ROWS: Row[] = [
  // ---------------- CORE ----------------
  ["py-core", "List versus tuple versus set", "Easy", 15, 88, 84, "amazon,flipkart,walmart,accenture", "mutability,hashing,lookup cost",
    "Compare list, tuple, and set on mutability, ordering, and lookup cost.",
    "List: mutable, ordered, membership check is O(n). Tuple: immutable, ordered, hashable so it can be a dict key. Set: mutable, unordered, membership check is O(1) average and holds only unique hashable items.",
    "The O(1) set membership is the practical takeaway. Swapping a list for a set in a hot loop is the most common easy speedup."],
  ["py-core", "Mutable default argument", "Medium", 20, 84, 92, "amazon,google,meta", "default arguments,evaluation time,gotcha",
    "What does this print and why: def f(x, acc=[]): acc.append(x); return acc  called three times?",
    "It returns [1], [1,2], [1,2,3]. The default list is created once at function definition and shared across calls. Fix it with acc=None and acc = [] if acc is None inside the body.",
    "This is the single most asked Python gotcha. Defaults are evaluated once at definition time, not per call."],
  ["py-core", "Shallow versus deep copy", "Medium", 20, 82, 88, "amazon,meta,adobe", "copy,references,nested structures",
    "What is the difference between a shallow and a deep copy, and when does it bite you?",
    "A shallow copy duplicates the outer container but shares the inner objects, so mutating a nested list affects both. copy.deepcopy recursively duplicates everything.",
    "Assignment copies a reference, not the object at all. Slicing and list() give shallow copies."],
  ["py-core", "Dictionary methods and ordering", "Easy", 15, 80, 80, "amazon,flipkart,walmart", "dict,get,setdefault,insertion order",
    "How do you safely read a possibly-missing key, and are dicts ordered?",
    "Use d.get(key, default) or d.setdefault(key, default) to insert while reading. Since Python 3.7 dicts preserve insertion order as a language guarantee.",
    "defaultdict avoids the branch entirely when you are accumulating into lists or counters."],
  ["py-core", "Sorting with a key function", "Medium", 20, 88, 90, "amazon,meta,flipkart,walmart", "sorted,key,stability,multi-key",
    "Sort a list of dicts by department ascending then salary descending.",
    "sorted(rows, key=lambda r: (r['department'], -r['salary']))\n# For non-numeric descending keys, sort twice relying on stability:\nrows.sort(key=lambda r: r['salary'], reverse=True)\nrows.sort(key=lambda r: r['department'])",
    "Python sorts are stable, so a sequence of single-key sorts from least to most significant gives correct multi-key ordering."],
  ["py-core", "is versus ==", "Easy", 15, 74, 82, "amazon,google", "identity,equality,interning",
    "Explain the difference between is and == and when the distinction matters.",
    "== compares values via __eq__; is compares object identity. Use is only for None, True, False, and sentinel objects.",
    "Small integers and short strings are interned, so is can appear to work by accident and then fail on larger values."],
  ["py-core", "Args and kwargs", "Easy", 15, 70, 78, "amazon,adobe", "variadic arguments,unpacking",
    "Explain *args and **kwargs and show how to forward them.",
    "def wrapper(*args, **kwargs):\n    return target(*args, **kwargs)",
    "The star packs positional arguments into a tuple and the double star packs keyword arguments into a dict. The same syntax unpacks them at the call site."],
  ["py-core", "Generators versus lists", "Medium", 20, 82, 90, "amazon,google,meta,stripe", "generators,lazy evaluation,memory",
    "When would you use a generator instead of building a list, and what do you give up?",
    "Use one when the sequence is large or infinite and you consume it once. You give up indexing, len, and the ability to iterate twice.",
    "Generators hold constant memory regardless of the sequence length, which is why they dominate file and stream processing."],
  ["py-core", "Decorator basics", "Medium", 20, 74, 86, "amazon,google,meta,stripe", "decorators,closures,functools.wraps",
    "Write a decorator that times a function call.",
    "import time, functools\n\ndef timed(fn):\n    @functools.wraps(fn)\n    def inner(*args, **kwargs):\n        t0 = time.perf_counter()\n        try:\n            return fn(*args, **kwargs)\n        finally:\n            print(f'{fn.__name__} took {time.perf_counter() - t0:.3f}s')\n    return inner",
    "functools.wraps preserves the name and docstring. Using try/finally means the timing still prints when the function raises."],
  ["py-core", "Context managers", "Medium", 20, 70, 84, "amazon,google,stripe", "with statement,resource cleanup,contextlib",
    "Why use a with block, and how do you write your own context manager?",
    "from contextlib import contextmanager\n\n@contextmanager\ndef opened(path):\n    f = open(path)\n    try:\n        yield f\n    finally:\n        f.close()",
    "The with block guarantees cleanup even when an exception is raised, which a manual close does not."],

  // ---------------- STRINGS ----------------
  ["py-strings", "String immutability and joining", "Medium", 20, 78, 86, "amazon,meta,flipkart", "immutability,join,performance",
    "Why is building a string with += in a loop slow, and what should you do instead?",
    "Strings are immutable, so each += allocates a new string and copies, giving quadratic behaviour. Collect the pieces in a list and call ''.join(parts) once.",
    "This quadratic-to-linear fix is a favourite performance question."],
  ["py-strings", "Common string methods", "Easy", 15, 74, 76, "amazon,walmart,accenture", "split,strip,replace,formatting",
    "Clean a messy input string: strip whitespace, collapse internal spaces, and title-case it.",
    "' '.join(s.split()).title()",
    "split() with no argument splits on any run of whitespace and drops empties, which makes the collapse a one-liner."],
  ["py-strings", "Counting characters", "Easy", 15, 76, 82, "amazon,flipkart", "Counter,frequency map",
    "Find the most common character in a string, ignoring spaces.",
    "from collections import Counter\nCounter(c for c in s if not c.isspace()).most_common(1)[0]",
    "Counter with a generator expression avoids materialising an intermediate list."],
  ["py-strings", "f-strings and formatting", "Easy", 15, 66, 70, "amazon,walmart", "f-strings,format spec",
    "Format a number as currency with two decimals and thousands separators.",
    "f'${value:,.2f}'",
    "The format mini-language handles padding, alignment, precision, and percentages without any helper library."],
  ["py-strings", "Regular expressions for extraction", "Medium", 20, 72, 82, "amazon,walmart,accenture", "re,groups,findall",
    "Extract all email domains from a block of text.",
    "import re\nre.findall(r'@([\\w.-]+\\.\\w+)', text)",
    "A capture group makes findall return just the domain. Compile the pattern once when using it in a loop."],

  // ---------------- COMPREHENSIONS ----------------
  ["py-comprehensions", "List, dict and set comprehensions", "Easy", 15, 86, 88, "amazon,flipkart,walmart,meta", "comprehensions,filtering,mapping",
    "Build a dict mapping each word to its length, keeping only words longer than three characters.",
    "{w: len(w) for w in words if len(w) > 3}",
    "Comprehensions are faster than an append loop because the append lookup is avoided. Readability is the real reason to prefer them."],
  ["py-comprehensions", "Nested comprehension and flattening", "Medium", 20, 74, 84, "amazon,meta", "nested loops,flattening,order",
    "Flatten a list of lists in one expression.",
    "[x for row in matrix for x in row]",
    "The loop order reads exactly as it would if written out as nested for statements, which is the mnemonic to remember."],
  ["py-comprehensions", "map, filter and lambda", "Medium", 20, 70, 78, "amazon,adobe", "functional,lambda,readability",
    "Rewrite map(lambda x: x*2, filter(lambda x: x % 2, nums)) as a comprehension and say which you prefer.",
    "[x * 2 for x in nums if x % 2]",
    "The comprehension is more readable and usually faster since it avoids the lambda call overhead. Reserve map for an existing named function."],
  ["py-comprehensions", "any, all and generator expressions", "Easy", 15, 68, 82, "amazon,google", "short circuit,any,all",
    "Check whether any row in a dataset has a missing value, without building an intermediate list.",
    "any(v is None for row in rows for v in row.values())",
    "any and all short circuit, so passing a generator rather than a list stops work at the first decisive element."],
  ["py-comprehensions", "enumerate and zip", "Easy", 15, 78, 80, "amazon,flipkart,walmart", "enumerate,zip,parallel iteration",
    "Iterate over two lists in parallel with an index.",
    "for i, (a, b) in enumerate(zip(xs, ys)):\n    ...",
    "zip stops at the shorter input. Use itertools.zip_longest when that truncation would silently lose data."],

  // ---------------- COLLECTIONS ----------------
  ["py-collections", "Counter for frequency analysis", "Easy", 15, 84, 88, "amazon,flipkart,meta,walmart", "Counter,most_common,arithmetic",
    "Find the top 5 most frequent products in a list of orders and the total distinct products.",
    "from collections import Counter\nc = Counter(order_products)\nc.most_common(5), len(c)",
    "Counter supports arithmetic, so c1 - c2 gives the difference in counts, which is handy for period-over-period comparison."],
  ["py-collections", "defaultdict for grouping", "Easy", 15, 82, 88, "amazon,flipkart,walmart", "defaultdict,grouping",
    "Group a list of (department, name) tuples into a dict of department to names.",
    "from collections import defaultdict\ng = defaultdict(list)\nfor dept, name in rows:\n    g[dept].append(name)",
    "defaultdict removes the key-existence check. Convert back with dict(g) if you want KeyError behaviour afterwards."],
  ["py-collections", "deque for queues and sliding windows", "Medium", 20, 74, 88, "amazon,uber,google", "deque,O(1) ends,maxlen",
    "Why use a deque instead of a list for a queue?",
    "list.pop(0) is O(n) because every element shifts. collections.deque gives O(1) appends and pops at both ends, and maxlen gives a fixed-size rolling window for free.",
    "The maxlen trick is a neat one-liner for moving averages over a stream."],
  ["py-collections", "itertools for combinatorics", "Medium", 20, 66, 82, "amazon,google,de-shaw", "itertools,combinations,groupby",
    "Generate all pairs from a list and group consecutive equal items.",
    "from itertools import combinations, groupby\nlist(combinations(items, 2))\n[(k, len(list(g))) for k, g in groupby(sorted(items))]",
    "groupby only groups consecutive runs, so the input must be sorted first. That catches many people out."],
  ["py-collections", "namedtuple and dataclass", "Medium", 20, 62, 78, "amazon,stripe", "namedtuple,dataclass,records",
    "When would you use a dataclass over a plain dict for records?",
    "When the fields are fixed and known. You get attribute access, type hints, a readable repr, and equality for free, with typos caught rather than silently creating a new key.",
    "namedtuple is the immutable, tuple-compatible alternative and is more memory efficient at scale."],

  // ---------------- PANDAS ----------------
  ["py-pandas", "groupby with multiple aggregations", "Medium", 20, 98, 96, "amazon,flipkart,walmart,swiggy,meta", "groupby,agg,named aggregation",
    "For each category, compute total revenue, average order value, and distinct customer count in one call.",
    "df.groupby('category').agg(\n    revenue=('amount', 'sum'),\n    aov=('amount', 'mean'),\n    customers=('user_id', 'nunique'),\n).reset_index()",
    "Named aggregation gives flat, readable column names instead of a MultiIndex. This is the single most used pandas pattern in analyst work."],
  ["py-pandas", "merge and join semantics", "Medium", 20, 96, 94, "amazon,flipkart,walmart,swiggy", "merge,how,validate,indicator",
    "Join orders to users keeping all orders, and verify you did not accidentally duplicate rows.",
    "out = orders.merge(users, on='user_id', how='left', validate='many_to_one', indicator=True)\nassert len(out) == len(orders)\nprint(out['_merge'].value_counts())",
    "The validate argument catches an unexpected many-to-many join immediately, which is exactly the bug that silently inflates totals."],
  ["py-pandas", "pivot_table versus groupby", "Medium", 20, 88, 90, "amazon,walmart,flipkart,accenture", "pivot_table,reshaping,fill_value",
    "Build a matrix of revenue by category (rows) and month (columns).",
    "df.pivot_table(index='category', columns='month', values='amount',\n                aggfunc='sum', fill_value=0)",
    "pivot_table aggregates duplicates; pivot raises on them. fill_value turns absent combinations into zeros rather than NaN."],
  ["py-pandas", "melt for long format", "Medium", 20, 76, 88, "amazon,walmart,accenture", "melt,wide to long,tidy data",
    "Convert a wide table with one column per month into long format.",
    "df.melt(id_vars=['category'], var_name='month', value_name='revenue')",
    "Long format is what plotting and modelling libraries expect. melt and pivot_table are inverses of each other."],
  ["py-pandas", "transform versus agg versus apply", "Medium", 20, 84, 94, "amazon,meta,flipkart,stripe", "transform,broadcast,performance",
    "Add a column giving each row's share of its category total.",
    "df['share'] = df['amount'] / df.groupby('category')['amount'].transform('sum')",
    "transform returns a series aligned to the original index, so no merge is needed. agg collapses to one row per group; apply is the slow general fallback."],
  ["py-pandas", "Window calculations", "Medium", 20, 86, 92, "amazon,uber,stripe,spotify", "rolling,expanding,shift",
    "Compute a 7-day rolling average of daily revenue and the day-over-day change.",
    "s = df.set_index('date')['revenue'].sort_index()\ns.rolling(7, min_periods=1).mean()\ns.pct_change()",
    "min_periods controls what happens at the start. Without sorting the index first, rolling silently produces nonsense."],
  ["py-pandas", "Per-group ranking and top-N", "Medium", 20, 88, 94, "amazon,flipkart,walmart,meta", "rank,nlargest,groupby head",
    "Get the top 3 products by revenue within each category.",
    "(df.sort_values('revenue', ascending=False)\n   .groupby('category')\n   .head(3))",
    "sort then groupby.head is the fastest idiom. groupby.rank(method='dense') is the alternative when you need the rank number itself."],
  ["py-pandas", "SettingWithCopyWarning", "Medium", 20, 82, 90, "amazon,flipkart,walmart", "views,copies,loc",
    "Explain SettingWithCopyWarning and how to avoid it.",
    "It appears when you assign into what may be a view of another frame, so the write may not propagate. Take an explicit copy after filtering, or assign with a single .loc call.\n\nsub = df[df.x > 0].copy()\nsub['y'] = 1\n# or\ndf.loc[df.x > 0, 'y'] = 1",
    "Chained indexing is the root cause. A single .loc assignment is unambiguous and always correct."],
  ["py-pandas", "loc versus iloc", "Easy", 15, 84, 86, "amazon,flipkart,walmart,accenture", "label indexing,positional indexing",
    "Explain the difference between loc and iloc, including slice endpoint behaviour.",
    "loc indexes by label and its slices are inclusive of the endpoint. iloc indexes by integer position and its slices exclude the endpoint like normal Python.",
    "The inclusive endpoint on loc surprises almost everyone once. It follows from labels not being positions."],
  ["py-pandas", "Handling duplicates", "Easy", 15, 82, 86, "amazon,walmart,flipkart,accenture", "duplicated,drop_duplicates,subset",
    "Find and remove duplicate customer records, keeping the most recent.",
    "df.sort_values('updated_at').drop_duplicates(subset=['user_id'], keep='last')\n# inspect first:\ndf[df.duplicated(subset=['user_id'], keep=False)]",
    "Always inspect duplicates before dropping. keep=False shows every member of each duplicate group, not just the extras."],
  ["py-pandas", "Chained operations and pipe", "Medium", 20, 70, 84, "stripe,spotify,amazon", "method chaining,pipe,readability",
    "Rewrite a sequence of intermediate assignments as a single readable chain.",
    "(df\n   .query('status == \"delivered\"')\n   .assign(revenue=lambda d: d.qty * d.price)\n   .groupby('category', as_index=False)['revenue'].sum()\n   .sort_values('revenue', ascending=False))",
    "assign with a lambda lets you reference columns created earlier in the same chain, which is what makes long chains possible."],
  ["py-pandas", "Memory optimisation", "Medium", 20, 68, 84, "amazon,walmart,stripe", "dtypes,category,downcasting",
    "A 5 GB CSV will not fit in memory. What do you do?",
    "Read only the needed columns with usecols, specify dtypes to avoid object columns, convert low-cardinality strings to category, downcast numerics, and process in chunks with chunksize. Consider Parquet or a columnar engine instead of CSV.",
    "Converting a repeated string column to category often cuts memory by an order of magnitude on its own."],

  // ---------------- NUMPY ----------------
  ["py-numpy", "Vectorisation over loops", "Medium", 20, 84, 92, "amazon,meta,de-shaw,stripe", "vectorisation,broadcasting,performance",
    "Why is a NumPy operation much faster than the equivalent Python loop?",
    "NumPy stores homogeneous data in contiguous memory and runs the loop in compiled C, avoiding per-element Python object overhead and enabling SIMD instructions.",
    "The rule of thumb is one to two orders of magnitude. If you are writing a loop over a NumPy array, look for the vectorised form first."],
  ["py-numpy", "Broadcasting rules", "Medium", 20, 76, 90, "amazon,meta,google", "broadcasting,shapes,alignment",
    "Explain broadcasting and predict the result shape of (3,1) plus (1,4).",
    "Shapes are aligned from the right; dimensions must match or be 1, and size-1 dimensions are stretched. The result is (3,4).",
    "Broadcasting avoids materialising intermediate copies. Accidental broadcasting is also a common source of silent bugs."],
  ["py-numpy", "Boolean masking and where", "Easy", 15, 78, 84, "amazon,flipkart,de-shaw", "boolean indexing,np.where",
    "Cap all values above 100 at 100 without a loop.",
    "arr = np.where(arr > 100, 100, arr)\n# or arr.clip(max=100)",
    "Boolean masks are also assignable: arr[arr > 100] = 100 modifies in place."],
  ["py-numpy", "Axis semantics", "Medium", 20, 74, 86, "amazon,meta,walmart", "axis,reduction,aggregation",
    "For a 2D array, what do axis=0 and axis=1 mean in a sum?",
    "axis=0 collapses rows and gives one value per column. axis=1 collapses columns and gives one value per row.",
    "The mnemonic: axis is the dimension that disappears. This applies identically in pandas."],
  ["py-numpy", "Views versus copies", "Medium", 20, 66, 84, "amazon,de-shaw", "views,copies,memory aliasing",
    "Does slicing a NumPy array copy the data?",
    "No. Basic slicing returns a view sharing the same buffer, so writing to the slice modifies the original. Fancy indexing with a list or boolean mask returns a copy.",
    "This differs from list slicing, which always copies. Aliasing bugs from this are hard to track down."],

  // ---------------- CLEANING ----------------
  ["py-cleaning", "Auditing a new dataset", "Medium", 20, 92, 92, "amazon,walmart,flipkart,accenture,mu-sigma", "profiling,missingness,dtypes",
    "You are handed an unfamiliar CSV. What are your first ten minutes?",
    "df.shape, df.dtypes, df.head(), df.describe(include='all'), df.isna().mean().sort_values(ascending=False), df.nunique(), and duplicate checks on the presumed key. Then validate ranges and cross-field consistency.",
    "Interviewers want a systematic checklist, not ad hoc poking. Naming the key and verifying its uniqueness is the step most candidates skip."],
  ["py-cleaning", "Missing value strategy in pandas", "Medium", 20, 88, 90, "amazon,walmart,flipkart,accenture", "fillna,dropna,imputation",
    "Show three ways to handle missing values and say when each applies.",
    "df.dropna(subset=['key_col'])                       # missing key is unusable\ndf['amount'] = df['amount'].fillna(df['amount'].median())\ndf['amount_missing'] = df['amount'].isna().astype(int)  # keep the signal",
    "Adding the missingness indicator before imputing preserves information that would otherwise be erased."],
  ["py-cleaning", "Type coercion problems", "Medium", 20, 80, 86, "amazon,walmart,accenture", "to_numeric,errors,dtype",
    "A numeric column loaded as object because of stray text. How do you fix it and find the offenders?",
    "bad = df.loc[pd.to_numeric(df['amount'], errors='coerce').isna() & df['amount'].notna()]\ndf['amount'] = pd.to_numeric(df['amount'], errors='coerce')",
    "Inspect the offending values before coercing. Silently turning them into NaN can hide a systematic upstream problem."],
  ["py-cleaning", "Standardising inconsistent categories", "Medium", 20, 78, 84, "walmart,accenture,flipkart,mu-sigma", "normalisation,mapping,fuzzy matching",
    "A city column contains Mumbai, mumbai, Bombay, and MUMBAI. How do you consolidate it?",
    "s = df['city'].str.strip().str.lower()\ndf['city_clean'] = s.replace({'bombay': 'mumbai'})\n# for long tails, use a curated mapping table or rapidfuzz for candidate matches",
    "Case folding handles the easy cases; genuine aliases need a mapping table. Never fuzzy-match without a human reviewing the pairs."],
  ["py-cleaning", "Validating a join key", "Medium", 20, 82, 90, "amazon,walmart,stripe", "key uniqueness,join validation,orphans",
    "Before joining two tables, what do you check?",
    "Key uniqueness on the dimension side, dtype match on both sides, null keys, whitespace or case differences, and the count of orphan keys that will not match.",
    "Doing this before the join turns a silent row-count explosion into a caught error."],
  ["py-cleaning", "Outlier treatment in a pipeline", "Medium", 20, 74, 86, "amazon,american-express,walmart", "winsorising,clipping,robustness",
    "Show how to winsorise a column at the 1st and 99th percentiles.",
    "lo, hi = df['amount'].quantile([0.01, 0.99])\ndf['amount_w'] = df['amount'].clip(lo, hi)",
    "Compute the bounds on training data only and reuse them at scoring time, or you have leaked test information."],

  // ---------------- DATETIME ----------------
  ["py-datetime", "Parsing and timezone handling", "Medium", 20, 82, 88, "amazon,uber,stripe,spotify", "to_datetime,timezones,UTC",
    "Parse a timestamp column and convert it to a local timezone.",
    "df['ts'] = pd.to_datetime(df['ts'], utc=True, errors='coerce')\ndf['ts_local'] = df['ts'].dt.tz_convert('Asia/Kolkata')",
    "Store in UTC and convert only for display. Mixing naive and aware timestamps is a frequent production bug."],
  ["py-datetime", "Resampling a time series", "Medium", 20, 84, 90, "uber,stripe,spotify,amazon", "resample,frequency,aggregation",
    "Convert per-event rows into daily and weekly totals.",
    "s = df.set_index('ts')['amount']\ns.resample('D').sum()\ns.resample('W-MON').sum()",
    "resample needs a DatetimeIndex. Specifying the week anchor matters because business weeks rarely start on Sunday."],
  ["py-datetime", "Date component features", "Easy", 15, 76, 82, "uber,swiggy,amazon,walmart", "dt accessor,components",
    "Extract day of week, hour, and a weekend flag from a timestamp column.",
    "df['dow'] = df['ts'].dt.dayofweek\ndf['hour'] = df['ts'].dt.hour\ndf['is_weekend'] = df['dow'].isin([5, 6])",
    "dayofweek is Monday-zero. Getting that backwards silently mislabels every weekend flag."],
  ["py-datetime", "Time differences and durations", "Easy", 15, 74, 80, "uber,doordash,amazon", "timedelta,duration,total_seconds",
    "Compute delivery time in minutes between two timestamp columns.",
    "df['minutes'] = (df['delivered_at'] - df['ordered_at']).dt.total_seconds() / 60",
    "Subtracting datetimes gives a timedelta. Use total_seconds rather than the seconds attribute, which only holds the sub-day remainder."],
  ["py-datetime", "Filling gaps in a date series", "Medium", 20, 70, 86, "uber,stripe,walmart", "reindex,date_range,gap filling",
    "Your daily metrics table is missing days with no activity. Fill them with zeros.",
    "idx = pd.date_range(df['date'].min(), df['date'].max(), freq='D')\ndf = df.set_index('date').reindex(idx, fill_value=0).rename_axis('date').reset_index()",
    "Missing days silently distort rolling averages, which is why gap filling comes before any window calculation."],

  // ---------------- CODING PROBLEMS ----------------
  ["py-coding", "Two sum in Python", "Easy", 15, 88, 88, "amazon,flipkart,walmart", "hash map,one pass",
    "Return the indices of two numbers adding to a target.",
    "def two_sum(nums, target):\n    seen = {}\n    for i, n in enumerate(nums):\n        if target - n in seen:\n            return [seen[target - n], i]\n        seen[n] = i\n    return []",
    "Analyst screens ask the same classics as engineering screens, just fewer of them. Know the hash-map ones cold."],
  ["py-coding", "Group records by a key", "Easy", 15, 84, 86, "amazon,flipkart,walmart", "grouping,defaultdict",
    "Given a list of (user, amount) tuples, return total spend per user sorted descending.",
    "from collections import defaultdict\ntotals = defaultdict(float)\nfor user, amt in rows:\n    totals[user] += amt\nsorted(totals.items(), key=lambda kv: -kv[1])",
    "This is the pure-Python version of a groupby. It appears constantly when interviewers ban pandas."],
  ["py-coding", "Find duplicates in a list", "Easy", 15, 78, 82, "amazon,walmart,accenture", "set,duplicate detection",
    "Return all values that appear more than once, preserving first-seen order.",
    "seen, dupes = set(), []\nfor x in items:\n    if x in seen and x not in dupes:\n        dupes.append(x)\n    seen.add(x)",
    "Using Counter is shorter but does not preserve order. Ask which the interviewer wants."],
  ["py-coding", "Moving average over a stream", "Medium", 20, 76, 88, "uber,amazon,stripe", "deque,rolling window",
    "Implement a class returning the moving average of the last k values.",
    "from collections import deque\n\nclass MovingAverage:\n    def __init__(self, k):\n        self.q = deque(maxlen=k)\n    def next(self, val):\n        self.q.append(val)\n        return sum(self.q) / len(self.q)",
    "maxlen evicts automatically. For large k, keep a running sum instead of re-summing each call."],
  ["py-coding", "Merge overlapping intervals", "Medium", 20, 82, 90, "amazon,meta,uber,flipkart", "sorting,intervals,merging",
    "Merge a list of overlapping (start, end) ranges.",
    "def merge(iv):\n    out = []\n    for s, e in sorted(iv):\n        if out and s <= out[-1][1]:\n            out[-1][1] = max(out[-1][1], e)\n        else:\n            out.append([s, e])\n    return out",
    "Sorting by start is what makes a single pass sufficient. This exact problem shows up in scheduling and sessionisation work."],
  ["py-coding", "Top k frequent items", "Medium", 20, 84, 88, "amazon,meta,flipkart", "Counter,heap,top-k",
    "Return the k most frequent elements in a list.",
    "from collections import Counter\nimport heapq\nc = Counter(items)\nheapq.nlargest(k, c, key=c.get)",
    "Counter.most_common(k) is the shortest form. Mentioning the O(n log k) heap alternative shows you understand the cost."],
  ["py-coding", "Reading a large file line by line", "Medium", 20, 74, 86, "amazon,stripe,walmart", "streaming,generators,memory",
    "Count matching lines in a 10 GB log without loading it into memory.",
    "with open(path) as f:\n    n = sum(1 for line in f if 'ERROR' in line)",
    "Iterating a file object streams it lazily. readlines would try to materialise all 10 GB."],
  ["py-coding", "Flatten a nested dictionary", "Medium", 20, 68, 84, "amazon,stripe,meta", "recursion,nested structures",
    "Flatten arbitrarily nested dicts into dotted keys.",
    "def flatten(d, prefix=''):\n    out = {}\n    for k, v in d.items():\n        key = f'{prefix}{k}'\n        if isinstance(v, dict):\n            out.update(flatten(v, key + '.'))\n        else:\n            out[key] = v\n    return out",
    "This comes up constantly when normalising JSON API responses into tabular form."],
];

export const PYTHON_QUESTIONS: SeedQuestion[] = ROWS.map((r, i) => {
  const [topic, title, difficulty, minutes, frequency, patternValue, companies, concepts, prompt, solution, explanation] = r;
  return {
    id: `py-${String(i + 1).padStart(3, "0")}`,
    category: "Python",
    topic,
    title,
    difficulty,
    estimatedMinutes: minutes,
    source: "pandas docs",
    sourceUrl: "https://pandas.pydata.org/docs/user_guide/index.html",
    sourceNote: "Standard interview concept; wording and worked answer are original to this app.",
    companyTags: companies ? companies.split(",") : [],
    frequencyScore: frequency,
    patternValue,
    conceptCoverage: Math.round((frequency + patternValue) / 2),
    concepts: concepts.split(","),
    skillsTested: concepts.split(","),
    prompt,
    solution,
    explanation,
  };
});
